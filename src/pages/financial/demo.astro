---
import ApiDemo from "~/components/ui/ApiDemo";
import Headline from "~/components/ui/Headline.astro";
import WidgetWrapper from "~/components/ui/WidgetWrapper.astro";
import Layout from "~/layouts/PageLayout.astro";

const metadata = {
  title: 'Services',
};

---

<Layout {...metadata}>
  <Headline title="Try Out the API" tagline="Financial Analysis API demo" />
  <WidgetWrapper containerClass={`border-2 border-stone-500 rounded-md mb-8`}>
      <ApiDemo client:load apiUrl="https://hft.unsuspicious.org/demo" method="GET"/>
  </WidgetWrapper>
</Layout>